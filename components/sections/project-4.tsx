import { doto } from '@/lib/fonts';

export function Project4() {
  return (
    <section id='project-4' className='h-screen flex items-center justify-center snap-start'>
      <h2 className={`text-4xl font-bold ${doto.className}`}>toy lexicon</h2>
    </section>
  );
}
