import { shrikhand } from '@/lib/fonts';

export function Project2() {
  return (
    <section id='project-2' className='h-screen flex items-center justify-center snap-start'>
      <h2 className={`text-4xl font-bold ${shrikhand.className}`}>retrofitted</h2>
    </section>
  );
}
